# requirements.txt
# Pinned to versions installed in venv/ as of 2026-08-17 — regenerate with
# `pip freeze` and re-curate to this list of directly-imported packages
# if you upgrade, don't blindly overwrite (this repo does not need every
# transitive dependency pinned here).

# Data pipeline
nba_api==1.11.4
pandas==3.0.3
psycopg2-binary==2.9.12
sqlalchemy==2.0.49
python-dotenv==1.2.2

# AI / LLM analysis layer
ollama==0.6.2
chromadb==1.5.9
json_repair==0.61.4

# Review UI
streamlit==1.59.2

# ML (win-probability model)
scikit-learn==1.9.0
numpy==2.4.4

# Utilities
loguru==0.7.3
tenacity==9.1.4
